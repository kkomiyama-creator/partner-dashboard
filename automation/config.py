# -*- coding: utf-8 -*-
"""強化対象判定・差分表示の閾値。運用に応じてここだけ変更すればよい（コード本体は触らない）。
根拠: 役員会資料(2026/07/22「受注計画とKPIコミット」) — 測れる指標（アポ数・成約率）だけで判断する方針。
測れないもの（商談の中身・失注理由など）に依存する判定はここに追加しないこと。
"""

# アポ→成約率の基準値（%）。これを下回ると「質が低い」とみなす目安（暫定値・変更可）。
RATE_BASELINE_PCT = 18.5

# 「量はあるが質が低い」を判定する際に対象とするアポ数の下限（母数が小さすぎる会社を誤判定しないため）。
QUALITY_CHECK_MIN_APO = 5

# 前期間比で「量が落ちている」とみなすアポ数の変化率（%）。前期比でこの値以下なら該当。
APO_DROP_PCT = -30

# 前期間比で「質が落ちている」とみなす成約率の変化（ポイント差）。前期比でこの値以下なら該当。
RATE_DROP_PT = -8

# 「構造的な穴」＝期間中の成果がほぼゼロとみなすアポ数の上限。
STRUCTURAL_GAP_MAX_APO = 1

# 差分セルの色分け（急落=赤 / 伸長=緑）に使う変化率の閾値（%）。
DIFF_HIGHLIGHT_DROP_PCT = -20
DIFF_HIGHLIGHT_RISE_PCT = 20

# 運用列（Google Sheetで手動入力）の想定値。値そのものはSheet側で自由入力可（ここは参考表示用）。
STATUS_TAGS = ["強化対象", "要テコ入れ", "安定", "要フォロー", "取材候補"]
CAUSE_TAGS = ["人数減（稼働問題）", "生産性低下（真の失速）", "健全な移動（昇格等）", "条件変化（商流・与信等）"]

# 取材候補（個人別）の判定基準。「パートナーへの取材について」Notion準拠。
# 稼働開始3ヶ月以内の新人基準は入社日データが無いため未実装（既知の制約）。
INTERVIEW_TOP_RANK = 5          # 成約数ランキングでこの順位以内なら「獲得数上位」候補
INTERVIEW_GROWTH_PCT = 50       # 成約数の前期間比伸び率(%)がこれ以上なら「成長率が高い」候補

# 個人別の強化対象候補（営業マン個人）。会社単位のAPO_DROP_PCTとは別に定義（個人は変動が大きいため別値）。
PERSON_REINFORCEMENT_DROP_PCT = -30  # 成約数の前期間比変化率(%)がこれ以下なら「成約数が落ちている」候補

# Slack定性トピックス: チャンネル名フィルタ（このパターンを含むチャンネルすべてが対象）
SLACK_CHANNEL_FILTER = "02_sh"

# Slack定性トピックスの種別分類キーワード（優先順位順）。日報フォーマットを最優先で検出する。
SLACK_KIND_DAILY_REPORT_MARKERS = ["日報｜", "日報|", "日報 |"]
SLACK_KIND_KEYWORDS = {
    "実績報告": ["獲得報告", "アポイン", "クローザー名", "販売価格"],
    "所感・分析": ["振り返り", "所感", "課題感", "反省", "要因"],
    "意思決定": ["決定", "決裁", "承認しました", "承認します"],
}
